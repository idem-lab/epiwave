% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_observation_coverage.R
\name{plot_observation_coverage}
\alias{plot_observation_coverage}
\title{Visualise observation data coverage against the derived date axis}
\usage{
plot_observation_coverage(observations)
}
\arguments{
\item{observations}{either an already-stacked \code{epiwave_stacked_observations}
object (output of \code{stack_jurisdictions()}), or a single jurisdiction's raw
\code{epiwave_observation_model} (output of \code{define_observation_model()}),
which is stacked on its own to derive its axis first}
}
\value{
a ggplot object: a date x jurisdiction coverage tile per stream
}
\description{
Since \code{target_infection_dates} is emergent rather than
user-supplied (see \code{stack_jurisdictions()}), it's worth being able to
see what actually got derived and how each stream/jurisdiction's data
aligns to it before committing to a (potentially slow) \code{fit_waves()}
run -- this is the visual form of the alignment checks used throughout
the package's own tests.
}
